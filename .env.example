# Copy this file to `.env` and set values locally / on your host (do not commit `.env`).

# Rewrites: /api/v1/* → BACKEND_URL/api/v1/*
BACKEND_URL=https://backend-53fz.onrender.com

# Optional — browser calls API directly (backend must allow CORS):
# NEXT_PUBLIC_API_BASE_URL=
